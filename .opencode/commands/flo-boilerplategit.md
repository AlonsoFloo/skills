---
allowed-tools:
- Bash
- Read
- Grep
- Write
- Edit
description: Run Git repository boilerplate setup and cross-checking audit workflow
---

Execute the `flo-boilerplategit` workflow with the `flo-devops` agent by loading the `flo-boilerplategit` skill.